export interface Skill {
  title: string;
  tech: string[];
  data: string;
}

// Define the structure for a project
export interface Project {
  name: string;
  tech: string[];
  info: string;
  link: string;
}
